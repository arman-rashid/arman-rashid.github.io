name: Update CV from Website

on:
  schedule:
    - cron: '0 0 * * 1'   # every Monday at 00:00 UTC
  workflow_dispatch:      # manual trigger

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      # Remove the fetch step – we assume scholar_stats.json is already up‑to‑date

      - name: Generate CV
        run: python generate_cv.py

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add cv.html scholar_stats.json   # add stats file just in case it was also changed (e.g., if generate_cv.py updates it)
          git diff --staged --quiet || git commit -m "chore: update CV from website data"
          git push
